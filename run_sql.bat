@echo off
echo Running SQL migrations...
"c:\xampp\mysql\bin\mysql.exe" -u root -e "USE decathlon; DROP TABLE IF EXISTS email_campaigns; CREATE TABLE email_campaigns (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL, subject VARCHAR(255) NOT NULL, preview_text VARCHAR(255) NULL, type ENUM('newsletter','promotional','abandoned_cart','welcome','re_engagement','flash_sale') DEFAULT 'newsletter', status ENUM('draft','scheduled','sending','sent','paused','cancelled') DEFAULT 'draft', template TEXT NULL, audience ENUM('all','new_customers','inactive_customers','specific_segment') DEFAULT 'all', segment_ids JSON NULL, scheduled_at TIMESTAMP NULL, sent_at TIMESTAMP NULL, total_recipients INT DEFAULT 0, delivered_count INT DEFAULT 0, opened_count INT DEFAULT 0, clicked_count INT DEFAULT 0, unsubscribed_count INT DEFAULT 0, bounced_count INT DEFAULT 0, from_name VARCHAR(255) NULL, from_email VARCHAR(255) NULL, reply_to VARCHAR(255) NULL, tags JSON NULL, is_ab_test TINYINT(1) DEFAULT 0, created_by BIGINT UNSIGNED NULL, created_at TIMESTAMP NULL, updated_at TIMESTAMP NULL, deleted_at TIMESTAMP NULL, INDEX idx_status (status), INDEX idx_type (type), INDEX idx_scheduled (scheduled_at));" 2>&1
echo Email campaigns done.

"c:\xampp\mysql\bin\mysql.exe" -u root -e "USE decathlon; CREATE TABLE IF NOT EXISTS stock_movements (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY, product_id BIGINT UNSIGNED NOT NULL, variant_id BIGINT UNSIGNED NULL, type ENUM('purchase','sale','return','adjustment','transfer','damage','expired') DEFAULT 'adjustment', quantity INT NOT NULL, quantity_before INT DEFAULT 0, quantity_after INT DEFAULT 0, reference_type VARCHAR(255) NULL, reference_id INT NULL, notes TEXT NULL, cost_per_unit DECIMAL(10,2) NULL, location VARCHAR(255) NULL DEFAULT 'Main Warehouse', created_by BIGINT UNSIGNED NULL, created_at TIMESTAMP NULL, updated_at TIMESTAMP NULL, INDEX idx_product (product_id), INDEX idx_type (type), INDEX idx_created (created_at), FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE);" 2>&1
echo Stock movements done.
echo All done!
